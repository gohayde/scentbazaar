{
  "name": "scent-bazaar-website",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=scent-bazaar",
    "deploy:preview": "wrangler pages deploy . --project-name=scent-bazaar --branch=preview"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
